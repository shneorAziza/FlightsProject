<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <%- include('partials/bootstrapLink') %>
</head>

<body>
    <div class="container mt-5">
        <h2 class="mb-4">Flight Information Form</h2>
        <form action="/updateflight/<%= flight.id %>" method="post">
            <div class="form-group">
                <label for="id">ID</label>
                <input type="number" class="form-control" id="id" name="id" value="<%= flight.id %>" readonly>
            </div>
            <div class="form-group">
                <label for="departure_time">Departure Time</label>
                <input type="datetime-local" class="form-control" id="departure_time" name="departure_time"
                    value="2024-06-06T10:00:00">
            </div>
            <div class="form-group">
                <label for="name">Name</label>
                <input type="text" class="form-control" id="name" name="name" value="">
            </div>
          
            <div class="mb-3">
                <label for="origin_country" class="form-label">Origin Country</label>
                <select class="form-select" id="origin_country" name="origin_country">
                    <% if(countries.length> 0 ){ %>
                       <% for (const country of countries) { %>
                        <option <%= country.id===flight.origin_country_id ? 'selected' : '' %> value="<%= country.id %>"><%= country.name %></option>
                       <% } %>
                        <% } %>
                </select>
            </div>
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
    </div>
</body>

</html>